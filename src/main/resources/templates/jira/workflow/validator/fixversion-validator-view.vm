$i18n.getText("fixversion-validator.view.label"):
#foreach ($resolution in ${submittedResolutions})
    <strong>$resolution.name</strong>#if ($velocityCount != ${submittedResolutions.size()}), #end
#end